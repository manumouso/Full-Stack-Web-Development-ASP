@model IEnumerable<Educacion.Models.AlumnosMateria>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        @*<th>
            @Html.DisplayNameFor(model => model.Funcion)
        </th>*@
        <th>
            @Html.DisplayNameFor(model => model.Alumno.Dni)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Materia.Codigo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    
        @*<td>
                @Html.DisplayFor(modelItem => item.Funcion)
            </td>*@
        <td>
            @Html.DisplayFor(modelItem => item.Alumno.Dni)
        </td>
    <td>
        @Html.DisplayFor(modelItem => item.Materia.Codigo)
    </td>
    <td>
       
        @Html.ActionLink("Delete", "Delete", new { id = item.Alumno.ID, id2 = item.Materia.ID })
    </td>
    
</tr>
}

</table>
